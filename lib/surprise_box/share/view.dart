import 'package:flutter/material.dart';

class SharePrizeView extends StatelessWidget {
  const SharePrizeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
